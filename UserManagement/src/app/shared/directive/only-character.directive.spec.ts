import { OnlyCharacterDirective } from './only-character.directive';

describe('OnlyCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyCharacterDirective(null);
    expect(directive).toBeTruthy();
  });
});
