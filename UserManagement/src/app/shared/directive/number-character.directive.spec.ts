import { NumberCharacterDirective } from './number-character.directive';

describe('NumberCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberCharacterDirective(null);
    expect(directive).toBeTruthy();
  });
});
